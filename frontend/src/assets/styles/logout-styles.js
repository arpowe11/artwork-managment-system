
const logoutStyles = {
  logoutButton: {
    padding: "10px 24px",
    marginTop: "20px",
    cursor: "pointer",
    backgroundColor: "#e63946", // softer red
    color: "#fff",
    border: "none",
    borderRadius: "8px",
    fontSize: "1rem",
    fontWeight: "600",
    transition: "all 0.3s ease", // smooth hover effect
    boxShadow: "0 4px 8px rgba(0, 0, 0, 0.15)",
  },
  logoutButtonHover: {
    backgroundColor: "#d62828", // deeper red on hover
    transform: "scale(1.05)",  // subtle zoom
    boxShadow: "0 6px 12px rgba(0, 0, 0, 0.2)",
  },
};

export default logoutStyles;
